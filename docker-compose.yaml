version: '3.8'

services:
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

#  example-app:
#    # Expo typically runs on the host in dev mode, so we might not dockerize it immediately
#    # But we can add it later for production builds
#    image: node:18-alpine
#    working_dir: /app
#    volumes:
#      - ./example-app:/app

